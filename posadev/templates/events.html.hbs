<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Events</title>
</head>
<body>
    <section id="hello">
        <h1>Events</h1>
        Events:
        <ul>
            {{#each events}}
            <li>ID: <a href="/events/{{ this.id }}">{{ this.id }}</a></li>
            <li>Name: {{ this.name }}</li>
            {{else}}
            <p> No events yet</p>
            {{/each}}
        </ul>
        <h3>Total: {{ len events }}</h3>
    </section>
</body>
</html>
